import styled from '@emotion/styled';

const ChartWrapper = styled.div``;
const DivSort = styled.div`
    display: flex;
    justify-content: flex-start;
    align-items: center;
    p {
        padding: 5px;
    }
`;

export { ChartWrapper, DivSort };
